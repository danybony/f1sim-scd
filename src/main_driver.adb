with Race; use Race;
with Race.Driver;
with Text_IO;
with ada.strings.unbounded;use ada.strings.unbounded;



procedure main_driver is
   use Text_IO;
   --pilota : Race.Driver.Driver_Ref_T;
begin

   --pilota:=new Race.Driver.the_Driver;

   put_line("Main Driver done...");
end main_driver;
